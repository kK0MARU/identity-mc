<UserControl x:Class="identity_launcher.Views.Components.ServersListComponent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:identity_launcher.Views.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border Style="{StaticResource ServersListBorder}">
            <ListBox ItemsSource="{Binding ServersList}"
                     SelectedItem="{Binding SelectedServer}"
                     SelectedIndex="2"
                     ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                     ScrollViewer.VerticalScrollBarVisibility="Hidden"
                     Background="Transparent"
                     BorderBrush="Transparent"
                     BorderThickness="0"
                     ItemContainerStyle="{StaticResource ServerChooseButton}"/>
        </Border>
    </Grid>
</UserControl>
